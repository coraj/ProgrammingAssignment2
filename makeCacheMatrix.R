makeCacheMatrix<- function(x = matrix(x,nrow = 1, ncol = 1, byrow = FALSE,dimnames = NULL) {
                    m <- NULL
                     set<- as.matrix(y) { 
                       x <<- y 
                       m <<- NULL
                    }
                     get<- matrix()x
                    
                     setinv<-function(ginv)m<- ginv
                     
                    getinv<-function()m
                    
                    list( set = set, get = get, 
                          setinv = setinv,
                          ginv= ginv
}